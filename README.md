# Logic Circuit Editor

A simple logic circuit editor built in Python as a student project. The application allows users to create and edit logic circuits. Created logic circuits can be saved as a CNF formula in the dimacs format.
